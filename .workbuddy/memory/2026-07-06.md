# 2026-07-06

- 已继续恢复 `ImportWorkbench.vue` 最后一层高风险功能：为解析预览区接回可编辑核心财务字段、重置为解析结果、调用 `api.confirmImport` 的确认导入动作；同时保留字段来源说明为只读展示，不恢复抽屉。修复了编辑字段索引类型与模式字面量的 TypeScript 报错，前端构建复查通过。
- 按用户要求对导入财报恢复工作做详细测试：前端 `npm run build` 通过；安装隔离 Python venv 后，重点后端测试 `tests/test_import_preview_sources.py` 与 `tests/test_pdf_financial_parser.py` 共3项通过；完整后端测试套件为47通过、13失败，失败主要来自历史页面契约/乱码断言、旧同步任务名预期、provider 列表预期和旧 `/api/imports/upload` 文本解析预期，与本轮 `ReportDocument` 上传/预览/确认恢复主链路不完全同源。
- 已处理完整测试剩余失败并完成验证：更新页面契约测试以匹配当前“股票池/导入财报/文档资产库”实现；修复 `parse_report_document` 避免复用同 hash 旧文档元数据覆盖新文本解析结果；修复 `pdf_financial_parser` 中紧凑字符串索引回映射，避免同一行中股票代码数字被误读为净利润。完整 `pytest D:/work/stock/tests -q` 结果为 60 passed，前端 `npm run build` 通过，仅保留既有 Rolldown pure annotation 与 chunk size warning。
