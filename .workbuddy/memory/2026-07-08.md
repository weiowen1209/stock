# 2026-07-08

- 已完成“同步所有股票”性能优化首轮：在 `backend/sync_service.py` 中为 `sync_quotes` 与 `sync_kline` 增加按股票受控并发（配置项 `SYNC_CONCURRENCY`，默认 6）；将 K 线逐条 upsert 改为批量 upsert（配置项 `SYNC_BATCH_SIZE`，默认 500）；将 provider health 成功/失败持久化改为支持 `auto_commit=False`，减少同步过程中高频事务提交。
- 同步增量起点计算由逐股查询改为批量汇总查询 `_build_incremental_starts`，避免全量同步时为每只股票重复查 `min/max/count`。验证结果：`tests/test_stage2.py` 与 `tests/test_stage5.py` 共 10 项通过，完整 `pytest D:/work/stock/tests -q` 60 项通过，前端 `npm run build` 通过，仅保留既有 Rolldown 注释与 chunk size warning。
- 股票池表格新增多选同步：表格增加选择列和“同步已选”批量操作，支持对所选股票执行增量同步或全量同步；单只股票同步能力保留，前端 Vue 类型检查通过。
- 按用户要求删除股票池列表中已失效的“同步”单列，保留顶部“同步已选”批量入口，避免表格展示无作用按钮。
- 复查单股同步残留逻辑：当前 `BaseData.vue` 已无单行同步按钮与相关局部状态；`App.vue` 和 `stockStore.ts` 仍保留单只同步分支与方法，属于首页“同步当前股票”等入口依赖，不建议删除。
- 核对同步实现一致性：前端单只/批量/全部同步最终都调用同一个后端 `/api/sync/trigger`；后端 `sync_quotes` 与 `sync_kline` 都用 `asyncio.Semaphore(settings.sync_concurrency)` + `asyncio.gather(...)` 并发拉取，区别只在传入股票代码数量不同，单只同步本质上是并发池里只有 1 个任务。
- 统一同步入口文案：将首页按钮、股票池批量下拉、同步进度弹窗和 K 线空态提示改为“同步股票范围/同步全部股票池/发起股票同步”等更一致表述，减少用户误以为三种入口走不同逻辑。
- 已修复“同步全部股票池”触发 500：根因是将 `AsyncSession` 直接用于并发 `asyncio.gather`，导致 `Session is already flushing` 和后续 `database is locked` / `PendingRollbackError`。当前已将 `sync_quotes` 与 `sync_kline` 回退为单 session 串行抓取+批量写库，保留 `_build_incremental_starts` 批量起点计算和 `_upsert_kline_rows` 批量 upsert，两类同步已可实际运行，完整测试恢复为 60 passed。
- 已撤回未完成的“安全并发”尝试代码，恢复 `backend/sync_service.py` 到稳定版本；实测 `default_stock_codes()` 取前 5 只股票时，`sync_quotes` 与 `sync_kline` 都能成功执行，同步相关测试 10 项通过、完整测试 60 项通过。后续若继续做并发，必须改为并发阶段不共享 `AsyncSession`，例如每任务独立会话或抓取阶段完全不写数据库。
- 已按方案 A 实现“并发抓取、串行落库”：`FailoverManager` 新增 `ProviderAttempt` 与 `execute_with_health_map()`，并发抓取阶段只基于预加载 `ProviderHealth` 决策，不再在协程内 `flush/commit` 数据库；抓取结束后由 `persist_provider_attempts()` 串行回写 provider 健康状态，再顺序写入 `RealtimeQuote` / `KLineData`。实测前 5 只股票：行情同步约 0.35s、日线同步约 1.59s；同步相关测试 10 项通过，完整测试 60 项通过。
- 已排查首页“行情数据截止 2026/7/5，覆盖 85/98”的原因：前端展示取的是 `RealtimeQuote.updated_at`，它反映数据库记录最后写入时间；而当前 13 只未覆盖股票是中国香港/美股代码（如 `HK00020`、`HK09880`、`US.TSLA.US`），现有行情 provider 不支持或请求失败，所以覆盖停在 85/98。为避免首页把数据库写入时间误当成行情源时间，已给 `RealtimeQuote` 新增 `source_updated_at` 字段、在同步写入时保存 provider 返回时间，并让首页优先展示 `quote_source_updated_at`。数据库迁移、前端构建、同步相关测试和完整测试均通过。
