# 2026-07-05

- 规划了“基本面深度分析”页签升级方案：将现有财务数据看板扩展为后端分析引擎 + 前端诊断台，包含财务质量评分、同比/环比拆解、杜邦分析、行业对比、估值分位和 AI 解读；建议分三期实施，先做确定性财务分析，再做行业/估值增强，最后接 AI 解读。
- 补充了“增长潜力”判断框架：增长空间、增长能力、增长质量、估值是否透支四段证据链；适合转化为基本面深度分析页签的增长潜力模块。
- 已将基本面深度分析设计落地到代码：新增 `/api/fundamentals/{code}/deep-analysis`，后端计算综合评分、增长潜力、财务质量、估值分位、同比/环比拆解、杜邦分析、行业对比和 AI 解读；前端 Fundamental 页签展示评分卡、AI 解读、评分因子、增长拆解、杜邦、行业对比和估值分位，并更新页面文档。
- 继续完善基本面深度分析：修正增长拆解口径，新增增长信号、评分等级/方向、杜邦估算 ROE 与解释、估值样本数和回归中枢空间；前端增加增长信号卡、评分等级、指标方向、杜邦注释和估值样本说明；后端测试与前端构建均通过。
- 更新首页页签：将“基础资料”改为“股票池”，将“导入工作台”改为“导入财报”，并调整“股票池”位于“导入财报”左侧。
- 技术分析页签的技术走势新增日线、周线、月线切换；切换时同步加载对应周期的 K 线和技术指标，前端类型检查与构建通过。
- 修复股票池覆盖导入重复提示：覆盖模式下 Excel 内重复股票按后出现记录覆盖，不再计入“重复股票”跳过提示；增量模式仍保留重复股票跳过提示，并补充后端测试。
- 同步当前股票增强为同步日线、周线、月线三类 K 线；后端同步接口支持 periods 列表，前端触发同步时传入 day/week/month，并通过 Python 语法检查与前端构建。
- 优化股票池页面空间：将“手工添加”合并进“股票池维护”卡片，与 Excel 导入通过小页签切换；前端 Vue 类型检查通过。
- 调整股票池手工添加分类录入：一级、二级、三级分类改为基于现有股票池分类的可搜索下拉选择，禁止手工输入新分类，并增加联动清空逻辑；前端类型检查通过。
- 排查周线/月线无数据显示：SQLite 只有 day 周期数据，原因是运行中的后端未使用新增 periods 字段；前端同步改为兼容式多请求，分别触发日线、周线、月线同步，构建通过。
- 新增股票池“分类维护”：后端增加 stock_categories 分类表与 CRUD 接口，导入/手工添加会同步分类；手工添加只能选择已维护分类；前端新增分类维护小页签，支持新增、修改、删除分类，并通过后端测试与前端类型检查。
- 当前基础股票池列表去掉“标签”列，新增每行“修改/删除”操作；修改复用手工添加表单，删除为软删除 is_active=False；后端新增 DELETE `/api/base-data/stocks/{code}`，前后端构建检查通过。
- 梳理了首页“同步当前股票”链路：DataStatusPanel 按钮触发 App.vue 中 store.syncCurrentStock；前端先同步当前 currentCode 的行情，再分别同步 day/week/month K线；后端通过 /api/sync/trigger 调用 sync_quotes/sync_kline，使用 sina/eastmoney provider failover 写入 realtime_quotes、kline_data 和 sync_log。
- 扩展股票池跨市场支持：导入/手工添加支持A股、港股和美股代码规范化，新增 stooq 美股行情/K线 provider，东方财富支持中国香港股票 secid，前端“同步当前股票”改为同步整个启用股票池的行情和日/周/月K线；后端股票池测试与相关 Python 语法检查通过，前端全量类型检查被 Fundamental.vue 现有语法错误阻断。
- 修复 Fundamental.vue 结构损坏与类型错误：重写 script/style 块，移除不存在的 Quote.change 字段引用；基本面深度分析页签顶部新增股票名称、代码、当前价和涨跌幅头部卡，解决“看不出分析哪只股票”的问题；前端构建 Fundamental.vue 部分通过，BaseData.vue 存在与本次无关的既有类型错误。
- 基本面页签新增股票模糊搜索：头部增加搜索框，支持按代码或名称模糊匹配股票池中的启用股票，选中后切换分析对象并自动加载该股票的深度分析数据；搜索结果下拉显示名称、代码和产业链标签，前端构建通过。
- 当前基础股票池表格每个数据列标题新增模糊查询输入框，支持按代码、名称、交易所、一级分类、二级/三级分类、核心产品组合筛选，并增加显示数量和清空筛选按钮；前端 Vue 类型检查通过。
- 改造“同步当前股票”进度展示：点击后弹出 SyncProgressDialog，显示进度条、当前步骤、数据源、股票代码、同步数量和步骤列表；后端 progress_hub 增加 current/total 字段，sync_quotes/sync_kline 发布步骤化文案；前端构建与后端语法检查通过。
- 修复股票池页签点击无响应风险：将表头筛选从脚本内动态组件改为模板内直接渲染 el-input，避免股票池组件挂载时潜在运行异常阻断页签切换；前端类型检查和生产构建均通过。
- 技术分析页签将“均线系统”整合进“技术走势”同一卡片：KLineChart 接收 technical 指标并在 K 线图上叠加收盘价、MA5、MA10、MA20；移除独立均线卡片，前端类型检查与构建通过。
- 首页数据状态面板新增“同步所有股票”按钮：前端增加 store.syncAllStocks，调用 `/api/sync/trigger` 不传 codes 以同步全部启用股票，复用 SyncProgressDialog 并区分当前/全部模式；前端构建通过。
- 核对 `机器人产业分类清单_终版.xlsx` 导入数量差异：Excel 有149行股票数据，其中148行可解析、1行新三板代码不支持；按股票代码去重后唯一股票为98只，覆盖导入显示98只是因为50行重复股票被同代码覆盖。
- 优化股票池导入跳过提示：跳过示例不再常驻，导入后仅临时显示 8 秒且支持手动关闭；导入结果统计卡仍保留跳过数量，前端类型检查与构建通过。
- 修复“同步所有股票”前端 15 秒超时：保留普通接口默认 15 秒超时，将 `/api/sync/trigger` 同步请求单独提高到 10 分钟，避免全量股票行情和多周期K线同步被 axios 默认超时中断；前端构建通过。
- 压缩同步进度弹窗高度：将垂直 el-steps 改为 4 个横向紧凑步骤块，减少 header、进度条、当前步骤和统计卡片间距及字号，使弹窗高度约缩短一半；前端构建通过。
- 股票池维护卡片将“同一模板”标签改为“模板下载”按钮；后端新增 `/api/base-data/stocks/template` 导出当前全部启用股票池为 Excel 导入模板，前端点击后下载 `股票池导入模板.xlsx`；后端测试、前端类型检查和构建通过。
- 排查并清理“保留分类”：来源是后端测试用例向真实 SQLite 写入的测试股票 `888888/保留股票/保留分类`；已将测试数据改成真实分类口径并从 `data/dashboard.db` 删除测试记录，复查无残留，后端测试通过。
- 修复同步进度弹窗无变化：原因是开发环境 Vite 代理未代理 WebSocket，前端连接 `5173/api/sync/progress` 收不到后端推送；改为开发端口下直连 `127.0.0.1:8000`，并为弹窗增加连接中/等待推送的 fallback 进度，前端构建通过。
- 技术走势卡片新增日期范围选择器，默认最近半年日期范围；K线、成交量、收盘价和 MA5/MA10/MA20 均按日期范围过滤，范围内无数据时显示提示；前端类型检查和构建通过。
- 排查同步弹窗显示“新增/更新0条、0/98”：数据库 provider_health 显示 sina/eastmoney/stooq 均临时不可用，导致 K线同步失败；同时修复完成态进度口径，失败时显示 failed 状态，K线完成文案改为成功股票数/总股票数与写入记录数分开展示，UnsupportedCapabilityError 不再污染 provider 健康状态；后端语法检查和前端构建通过。
- 增强同步失败提示：当所有错误均为 provider temporarily unavailable 时，后端进度明确显示“数据源不可用：当前没有可用数据源，请稍后重试或切换数据源”；前端弹窗失败态改为红色异常进度与红色提示框；后端语法检查和前端构建通过。
- 技术分析页签调整 MACD 和 RSI6 各自独占一整行；KLineChart 将日期范围向父组件同步，MACD/RSI6 按同一日期范围过滤，保持与 K 线图显示区间一致；前端类型检查和构建通过。
- 重新设计“导入财报”方向：将 PDF 财报作为可去重、可复用、可追溯的文档资产保存，再通过解析任务产出结构化财务指标、业务分部、费用结构和深度档案字段；后续基本面分析与个股档案读取已确认数据和原始 PDF，无需重复上传。
- 技术分析副图样式改为接近同花顺/通达信：MACD 以红绿柱表示正负值并叠加 DIF/DEA 线；RSI6 为单线展示并增加 20/50/80 虚线参考线；前端类型检查和构建通过。
- 首页数据状态面板去掉左侧数据拉取进度/进度条/provider 健康状态区域，仅保留股票池、行情、近2年日K、缺失数量和同步按钮，前端构建通过。
- 重新设计首页数据拉取进度区域：状态面板恢复为紧凑进度卡，显示当前文案、阶段、进度条、数据源、股票和数量，仅保留“同步所有股票”入口；移除首页“同步当前股票”按钮，股票池表格新增每行“同步”按钮，可对单只股票同步行情与K线并复用进度弹窗；前端构建通过。
- 首页状态面板改为“股票数据状态”指标看板：去掉等待同步/准备中文案，展示股票池数量、行情数据截止日期、K线数据范围、覆盖率、K线周期数和缺失数据；后端 sync_coverage 增加 quote_updated_at、kline_start_date/kline_end_date、kline_period_count 字段；后端语法检查和前端构建通过。
- 清理 K线数据范围异常到 2030-01-02：删除真实库中 source=page_flow_static 的未来日期测试K线记录，复查最大日期恢复为 2026-07-04；sync_coverage 计算K线范围时增加 date <= today 过滤，避免未来脏数据影响状态面板；后端语法检查通过。
- 产业链总览的产业链雷达卡片去掉"人形机器人A股股票池"标题，改为一级分类和二级分类两组筛选；Pinia store 新增 selectedSubIndustry/subIndustries 以及 setSelectedIndustry/setSelectedSubIndustry，股票列表按一级+二级分类联合过滤；前端构建通过。
- 产业链总览重新布局：去掉"股票池规模/产业链环节/行情记录"三个 MetricCard，雷达卡片改为全宽横向卡片，下方接股票列表；首页数据状态面板的"股票池数量"改为"股票数量"并副标题显示行情覆盖；前端构建通过。
- 产业链雷达卡片改为一级/二级/三级分类并列三列展示：新增 selectedSub2Industry/sub2Industries/setSelectedSub2Industry，按 industry_chain_detail 拆分"二级/三级"做三级联动筛选；切换上级时自动重置下级；窄屏下三列堆叠；前端构建通过。
- 已按“PDF财报资产库”方案实现导入财报重构：后端新增 report_documents/report_parse_jobs/report_parse_results，支持PDF hash去重保存、读取原文、重新解析、预览与确认入库；前端导入财报页改为上传复用、文档库和解析确认工作台，个股档案页新增财报原文列表入口。
- 接入专门财报三表解析层：新增 backend/pdf_financial_parser.py，通过利润表/资产负债表/现金流量表分区、字段同义词、单位换算、费用率/毛利率派生和字段来源记录，替换导入链路中的轻量正则解析；新增 tests/test_pdf_financial_parser.py 覆盖三表字段抽取，相关后端测试和前端类型检查通过。
- 用真实文件 `688017_绿的谐波_2025年报.pdf` 试解析：pypdf 可提取 233 页、约 17.8 万字符；初测发现毛利误取毛利率文本、净利润误取被合并方0，已修正规则为优先逐字段标签扫描、支持跨行净利润、营业成本优先于营业总成本；复测可提取收入570,714,025.26、归母净利124,366,913.57、经营现金流152,003,651.27、总资产3,929,127,498.81、净资产3,536,833,385.23、毛利率36.91%、研发费用率9.45%，后端测试通过。
- 正式将 pypdf==5.1.0 加入 requirements 并将 PDF 文本解析切到 pypdf 优先；财报解析新增主营业务分行业/分产品/分地区/分销售模式表解析，真实绿的谐波2025年报可提取12条业务分部（行业5、产品3、地区2、销售模式2），后端测试和前端类型检查通过。
- 导入财报页继续增强：后端 ImportPreview 增加 field_sources 输出，前端新增字段来源核对抽屉展示表区/命中标签/单位/原文行/置信度；解析预览区新增确认前可编辑字段表单，确认入库时使用修正后的字段；相关后端测试、前端类型检查和生产构建通过。
- 重新设计导入财报 UI：ImportWorkbench.vue 改为顶部流程说明 + 左侧上传/手工补录 + 右侧文档资产库/解析预览/导入批次的工作台布局，新增财务指标卡片、状态胶囊、选中文档高亮、金额/百分比格式化和响应式样式；前端类型检查与生产构建通过。
- 修复保存并解析PDF接口500：原因是新增 field_sources 后返回 ImportPreview 时 document.updated_at 仍处于未刷新状态，Pydantic 序列化触发 async lazy load MissingGreenlet；parse_report_document 在返回前 refresh document/job/batch，并为旧库 report_parse_results 补 field_sources_json 迁移；新增 tests/test_import_preview_sources.py 覆盖上传接口返回字段来源，后端测试、前端类型检查和构建通过。
