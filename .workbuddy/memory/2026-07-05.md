# 2026-07-05

- 规划了“基本面深度分析”页签升级方案：将现有财务数据看板扩展为后端分析引擎 + 前端诊断台，包含财务质量评分、同比/环比拆解、杜邦分析、行业对比、估值分位和 AI 解读；建议分三期实施，先做确定性财务分析，再做行业/估值增强，最后接 AI 解读。
- 补充了“增长潜力”判断框架：增长空间、增长能力、增长质量、估值是否透支四段证据链；适合转化为基本面深度分析页签的增长潜力模块。
- 已将基本面深度分析设计落地到代码：新增 `/api/fundamentals/{code}/deep-analysis`，后端计算综合评分、增长潜力、财务质量、估值分位、同比/环比拆解、杜邦分析、行业对比和 AI 解读；前端 Fundamental 页签展示评分卡、AI 解读、评分因子、增长拆解、杜邦、行业对比和估值分位，并更新页面文档。
- 继续完善基本面深度分析：修正增长拆解口径，新增增长信号、评分等级/方向、杜邦估算 ROE 与解释、估值样本数和回归中枢空间；前端增加增长信号卡、评分等级、指标方向、杜邦注释和估值样本说明；后端测试与前端构建均通过。
- 更新首页页签：将“基础资料”改为“股票池”，将“导入工作台”改为“导入财报”，并调整“股票池”位于“导入财报”左侧。
- 技术分析页签的技术走势新增日线、周线、月线切换；切换时同步加载对应周期的 K 线和技术指标，前端类型检查与构建通过。
- 修复股票池覆盖导入重复提示：覆盖模式下 Excel 内重复股票按后出现记录覆盖，不再计入“重复股票”跳过提示；增量模式仍保留重复股票跳过提示，并补充后端测试。
- 同步当前股票增强为同步日线、周线、月线三类 K 线；后端同步接口支持 periods 列表，前端触发同步时传入 day/week/month，并通过 Python 语法检查与前端构建。
- 优化股票池页面空间：将“手工添加”合并进“股票池维护”卡片，与 Excel 导入通过小页签切换；前端 Vue 类型检查通过。
- 调整股票池手工添加分类录入：一级、二级、三级分类改为基于现有股票池分类的可搜索下拉选择，禁止手工输入新分类，并增加联动清空逻辑；前端类型检查通过。
- 排查周线/月线无数据显示：SQLite 只有 day 周期数据，原因是运行中的后端未使用新增 periods 字段；前端同步改为兼容式多请求，分别触发日线、周线、月线同步，构建通过。
- 新增股票池“分类维护”：后端增加 stock_categories 分类表与 CRUD 接口，导入/手工添加会同步分类；手工添加只能选择已维护分类；前端新增分类维护小页签，支持新增、修改、删除分类，并通过后端测试与前端类型检查。
- 当前基础股票池列表去掉“标签”列，新增每行“修改/删除”操作；修改复用手工添加表单，删除为软删除 is_active=False；后端新增 DELETE `/api/base-data/stocks/{code}`，前后端构建检查通过。
- 梳理了首页“同步当前股票”链路：DataStatusPanel 按钮触发 App.vue 中 store.syncCurrentStock；前端先同步当前 currentCode 的行情，再分别同步 day/week/month K线；后端通过 /api/sync/trigger 调用 sync_quotes/sync_kline，使用 sina/eastmoney provider failover 写入 realtime_quotes、kline_data 和 sync_log。
- 扩展股票池跨市场支持：导入/手工添加支持A股、港股和美股代码规范化，新增 stooq 美股行情/K线 provider，东方财富支持中国香港股票 secid，前端“同步当前股票”改为同步整个启用股票池的行情和日/周/月K线；后端股票池测试与相关 Python 语法检查通过，前端全量类型检查被 Fundamental.vue 现有语法错误阻断。
- 修复 Fundamental.vue 结构损坏与类型错误：重写 script/style 块，移除不存在的 Quote.change 字段引用；基本面深度分析页签顶部新增股票名称、代码、当前价和涨跌幅头部卡，解决“看不出分析哪只股票”的问题；前端构建 Fundamental.vue 部分通过，BaseData.vue 存在与本次无关的既有类型错误。
- 基本面页签新增股票模糊搜索：头部增加搜索框，支持按代码或名称模糊匹配股票池中的启用股票，选中后切换分析对象并自动加载该股票的深度分析数据；搜索结果下拉显示名称、代码和产业链标签，前端构建通过。
- 当前基础股票池表格每个数据列标题新增模糊查询输入框，支持按代码、名称、交易所、一级分类、二级/三级分类、核心产品组合筛选，并增加显示数量和清空筛选按钮；前端 Vue 类型检查通过。
- 改造“同步当前股票”进度展示：点击后弹出 SyncProgressDialog，显示进度条、当前步骤、数据源、股票代码、同步数量和步骤列表；后端 progress_hub 增加 current/total 字段，sync_quotes/sync_kline 发布步骤化文案；前端构建与后端语法检查通过。
- 修复股票池页签点击无响应风险：将表头筛选从脚本内动态组件改为模板内直接渲染 el-input，避免股票池组件挂载时潜在运行异常阻断页签切换；前端类型检查和生产构建均通过。
- 技术分析页签将“均线系统”整合进“技术走势”同一卡片：KLineChart 接收 technical 指标并在 K 线图上叠加收盘价、MA5、MA10、MA20；移除独立均线卡片，前端类型检查与构建通过。
- 首页数据状态面板新增“同步所有股票”按钮：前端增加 store.syncAllStocks，调用 `/api/sync/trigger` 不传 codes 以同步全部启用股票，复用 SyncProgressDialog 并区分当前/全部模式；前端构建通过。
- 核对 `机器人产业分类清单_终版.xlsx` 导入数量差异：Excel 有149行股票数据，其中148行可解析、1行新三板代码不支持；按股票代码去重后唯一股票为98只，覆盖导入显示98只是因为50行重复股票被同代码覆盖。
- 优化股票池导入跳过提示：跳过示例不再常驻，导入后仅临时显示 8 秒且支持手动关闭；导入结果统计卡仍保留跳过数量，前端类型检查与构建通过。
- 修复“同步所有股票”前端 15 秒超时：保留普通接口默认 15 秒超时，将 `/api/sync/trigger` 同步请求单独提高到 10 分钟，避免全量股票行情和多周期K线同步被 axios 默认超时中断；前端构建通过。
- 压缩同步进度弹窗高度：将垂直 el-steps 改为 4 个横向紧凑步骤块，减少 header、进度条、当前步骤和统计卡片间距及字号，使弹窗高度约缩短一半；前端构建通过。
- 股票池维护卡片将“同一模板”标签改为“模板下载”按钮；后端新增 `/api/base-data/stocks/template` 导出当前全部启用股票池为 Excel 导入模板，前端点击后下载 `股票池导入模板.xlsx`；后端测试、前端类型检查和构建通过。
- 排查并清理“保留分类”：来源是后端测试用例向真实 SQLite 写入的测试股票 `888888/保留股票/保留分类`；已将测试数据改成真实分类口径并从 `data/dashboard.db` 删除测试记录，复查无残留，后端测试通过。
- 修复同步进度弹窗无变化：原因是开发环境 Vite 代理未代理 WebSocket，前端连接 `5173/api/sync/progress` 收不到后端推送；改为开发端口下直连 `127.0.0.1:8000`，并为弹窗增加连接中/等待推送的 fallback 进度，前端构建通过。
- 技术走势卡片新增日期范围选择器，默认最近半年日期范围；K线、成交量、收盘价和 MA5/MA10/MA20 均按日期范围过滤，范围内无数据时显示提示；前端类型检查和构建通过。
- 排查同步弹窗显示“新增/更新0条、0/98”：数据库 provider_health 显示 sina/eastmoney/stooq 均临时不可用，导致 K线同步失败；同时修复完成态进度口径，失败时显示 failed 状态，K线完成文案改为成功股票数/总股票数与写入记录数分开展示，UnsupportedCapabilityError 不再污染 provider 健康状态；后端语法检查和前端构建通过。
- 增强同步失败提示：当所有错误均为 provider temporarily unavailable 时，后端进度明确显示“数据源不可用：当前没有可用数据源，请稍后重试或切换数据源”；前端弹窗失败态改为红色异常进度与红色提示框；后端语法检查和前端构建通过。
- 技术分析页签调整 MACD 和 RSI6 各自独占一整行；KLineChart 将日期范围向父组件同步，MACD/RSI6 按同一日期范围过滤，保持与 K 线图显示区间一致；前端类型检查和构建通过。
- 重新设计“导入财报”方向：将 PDF 财报作为可去重、可复用、可追溯的文档资产保存，再通过解析任务产出结构化财务指标、业务分部、费用结构和深度档案字段；后续基本面分析与个股档案读取已确认数据和原始 PDF，无需重复上传。
- 技术分析副图样式改为接近同花顺/通达信：MACD 以红绿柱表示正负值并叠加 DIF/DEA 线；RSI6 为单线展示并增加 20/50/80 虚线参考线；前端类型检查和构建通过。
- 首页数据状态面板去掉左侧数据拉取进度/进度条/provider 健康状态区域，仅保留股票池、行情、近2年日K、缺失数量和同步按钮，前端构建通过。
- 重新设计首页数据拉取进度区域：状态面板恢复为紧凑进度卡，显示当前文案、阶段、进度条、数据源、股票和数量，仅保留“同步所有股票”入口；移除首页“同步当前股票”按钮，股票池表格新增每行“同步”按钮，可对单只股票同步行情与K线并复用进度弹窗；前端构建通过。
- 首页状态面板改为“股票数据状态”指标看板：去掉等待同步/准备中文案，展示股票池数量、行情数据截止日期、K线数据范围、覆盖率、K线周期数和缺失数据；后端 sync_coverage 增加 quote_updated_at、kline_start_date/kline_end_date、kline_period_count 字段；后端语法检查和前端构建通过。
- 清理 K线数据范围异常到 2030-01-02：删除真实库中 source=page_flow_static 的未来日期测试K线记录，复查最大日期恢复为 2026-07-04；sync_coverage 计算K线范围时增加 date <= today 过滤，避免未来脏数据影响状态面板；后端语法检查通过。
- 产业链总览的产业链雷达卡片去掉"人形机器人A股股票池"标题，改为一级分类和二级分类两组筛选；Pinia store 新增 selectedSubIndustry/subIndustries 以及 setSelectedIndustry/setSelectedSubIndustry，股票列表按一级+二级分类联合过滤；前端构建通过。
- 产业链总览重新布局：去掉"股票池规模/产业链环节/行情记录"三个 MetricCard，雷达卡片改为全宽横向卡片，下方接股票列表；首页数据状态面板的"股票池数量"改为"股票数量"并副标题显示行情覆盖；前端构建通过。
- 产业链雷达卡片改为一级/二级/三级分类并列三列展示：新增 selectedSub2Industry/sub2Industries/setSelectedSub2Industry，按 industry_chain_detail 拆分"二级/三级"做三级联动筛选；切换上级时自动重置下级；窄屏下三列堆叠。随后补上三级分类在二级为"全部"时也展示当前一级下全部三级选项。继续排查首页白屏时，进一步移除 `StockDetail.vue` 首屏对局部 `stock/quote` 包装值的依赖，模板与文档加载逻辑统一改为直接读取 `store.currentStock/currentQuote`，供应链标签改为从 `store.currentStock?.supply_chain_tags` 解析；同时将 `Fundamental.vue` 的 `deep` 取值显式兜底为 null。为隔离整页白屏组件，又将 `App.vue` 的非首个页签组件改为按 `activeTab` 惰性渲染，仅在切到对应页签时才挂载 `Fundamental/Technical/StockDetail/BaseData/ImportWorkbench`，便于确认是否是非当前页签首屏挂载把整页打白；前端重新构建通过，并重新打开 `http://127.0.0.1:5175` 做实际页面自测。
- 将项目代码提交并推送到 GitHub 仓库 https://github.com/weiowen1209/stock.git：git init → 关联远程 → 111 文件首次提交（commit a4a43a9）→ 通过 GitHub PAT API 完成推送，main 分支已与远程同步。
- 已按“PDF财报资产库”方案实现导入财报重构：后端新增 report_documents/report_parse_jobs/report_parse_results，支持PDF hash去重保存、读取原文、重新解析、预览与确认入库；前端导入财报页改为上传复用、文档库和解析确认工作台，个股档案页新增财报原文列表入口。
- 接入专门财报三表解析层：新增 backend/pdf_financial_parser.py，通过利润表/资产负债表/现金流量表分区、字段同义词、单位换算、费用率/毛利率派生和字段来源记录，替换导入链路中的轻量正则解析；新增 tests/test_pdf_financial_parser.py 覆盖三表字段抽取，相关后端测试和前端类型检查通过。
- 用真实文件 `688017_绿的谐波_2025年报.pdf` 试解析：pypdf 可提取 233 页、约 17.8 万字符；初测发现毛利误取毛利率文本、净利润误取被合并方0，已修正规则为优先逐字段标签扫描、支持跨行净利润、营业成本优先于营业总成本；复测可提取收入570,714,025.26、归母净利124,366,913.57、经营现金流152,003,651.27、总资产3,929,127,498.81、净资产3,536,833,385.23、毛利率36.91%、研发费用率9.45%，后端测试通过。
- 正式将 pypdf==5.1.0 加入 requirements 并将 PDF 文本解析切到 pypdf 优先；财报解析新增主营业务分行业/分产品/分地区/分销售模式表解析，真实绿的谐波2025年报可提取12条业务分部（行业5、产品3、地区2、销售模式2），后端测试和前端类型检查通过。
- 导入财报页继续增强：后端 ImportPreview 增加 field_sources 输出，前端新增字段来源核对抽屉展示表区/命中标签/单位/原文行/置信度；解析预览区新增确认前可编辑字段表单，确认入库时使用修正后的字段；相关后端测试、前端类型检查和生产构建通过。
- 重新设计导入财报 UI：ImportWorkbench.vue 改为顶部流程说明 + 左侧上传/手工补录 + 右侧文档资产库/解析预览/导入批次的工作台布局，新增财务指标卡片、状态胶囊、选中文档高亮、金额/百分比格式化和响应式样式；前端类型检查与生产构建通过。
- 修复保存并解析PDF接口500：原因是新增 field_sources 后返回 ImportPreview 时 document.updated_at 仍处于未刷新状态，Pydantic 序列化触发 async lazy load MissingGreenlet；parse_report_document 在返回前 refresh document/job/batch，并为旧库 report_parse_results 补 field_sources_json 迁移；新增 tests/test_import_preview_sources.py 覆盖上传接口返回字段来源，后端测试、前端类型检查和构建通过。
- 使用下载财报技能获取绿的谐波（688017）最近3个报告年度财报PDF，保存到 data/绿的谐波_PDF财报_最近3年，共13个PDF：2023-2025年年报/半年报/一季报/三季报，以及已披露的2026年一季报；清理误下载的2023年审计报告，并生成 download_manifest.json。

- 读取用户提供的《公司分析20：绿的谐波》DOCX，提炼其分析思路：以机器人产业必然性为宏观入口，按产品/行业双口径拆业务，重点拆毛利变化、费用/利得损失对净利润的影响、19项业绩影响因素排序、现金流/资产负债/股本扩张、专利研发产能、国产替代逻辑和估值定位；据此规划后续年报数据抽取清单与基本面分析框架供用户确认。
- 按已确认方案继续改造基本面分析：PDF解析新增营业利润、扣非净利、利得损失、销售收现、存货/资本公积/股本、研发投入、专利、在建工程和关键文本线索等扩展字段，并持久化 extractions_json；深度分析接口新增八模块、业务贡献、净利润影响因素排序和跟踪信号；Fundamental.vue 重构为投研框架展示。真实绿的谐波2024年报验证可提取约19.2万字符、12条分部、20类扩展字段；后端解析测试、Python语法检查和前端构建通过。
- 继续按建议将扩展字段正式资产化：新增 annual_report_extractions 确认表，确认导入时将 ReportExtractions 写入并与 code/report_period 唯一关联；导入财报页新增“深度分析字段核对”和文本线索编辑区，确认请求携带 extractions；基本面分析读取已确认扩展字段，用资本公积/股本/研发投入/专利/国家标准/产能项目增强八模块。后端语法检查、PDF解析测试和前端构建通过。
- 为已确认年报扩展字段新增查询闭环：后端增加 GET `/api/fundamentals/{code}/extractions`，返回 annual_report_extractions 并反序列化 notes_json；前端 API/Pinia store 接入 annualReportExtractions；个股深度档案页新增“年报深度字段”卡片，展示营业利润、扣非净利、减值、销售收现、存货、资本公积、股本、研发投入、专利、在建工程和文本线索。修复 ES2020 下 Array.at 类型错误，后端语法检查、PDF解析测试和前端构建通过。
- 修复“导入财报”页白屏：原因是 `frontend/src/views/ImportWorkbench.vue` 中在声明 `ExtractionValueKey` 之前就把它用于 `reactive<Record<ExtractionValueKey,...>>`，导致页签挂载时脚本进入 TDZ 运行时异常；已将类型声明提前到相关状态定义之前，前端生产构建复查通过。
- 收口首页白屏阶段的临时隔离实现：移除 `frontend/src/App.vue` 里非首屏页签仅在激活时才挂载的 `v-if` 惰性渲染，恢复为正式全量页签结构；保留 `Fundamental.vue`、`StockDetail.vue` 等页面已补上的空值兜底。随后复查前端，`vue-tsc --noEmit` 通过，`cd frontend && npm run build` 构建通过；直接调用 vite 二进制仍会出现当前环境下的 `index.html` entry 解析异常，因此以后以项目脚本构建结果为准。
- 继续修复“导入财报”页点击后 patchElement 报 `Cannot set properties of null (setting '__vnode')`：将深度字段和文本线索区域中的裸 `el-form-item` 包进正式 `el-form`，避免 Element Plus 表单项在 keyed diff 和焦点移除时直接操作已卸载节点；同时再次把 `ExtractionValueKey` 类型声明前移到相关 reactive 状态之前，前端构建复查通过。
- 用户反馈首页在移除页签惰性挂载后再次白屏，已立即将 `frontend/src/App.vue` 恢复为按 `activeTab` 惰性挂载 `Fundamental/Technical/StockDetail/BaseData/ImportWorkbench` 非首屏页签，作为稳定保护层；复跑 `cd frontend && npm run build` 通过。当前判断仍是某个非首屏页签在全量挂载时触发运行时异常，需后续逐页定位根因后再正式去掉隔离层。
- 继续排查“导入财报”页仍白屏：发现 `frontend/src/App.vue` 中 `el-tab-pane` 已经管理了页签内容显示，内部又对 `ImportWorkbench` 单独加 `v-if="activeTab === 'import'"`，与 Element Plus 页签切换的挂载/卸载节奏叠加后，仍可能在点击切换时触发 Vue patch 空节点；已改为给非首屏页签直接加 `lazy`，并移除页签内容内部的 `v-if`，让挂载生命周期交给 `el-tab-pane` 自己管理，前端构建通过。
- 继续逐页签定位首页白屏时，确认 `frontend/src/App.vue` 当前使用 Element Plus `el-tab-pane lazy` 作为保护层。顺序验证基本面、技术面、个股档案、股票池、导入财报在该结构下都能通过 `cd frontend && npm run build`，说明问题暂未表现为构建期/类型错误，更像浏览器运行时首屏挂载或 DOM patch 阶段异常；当前先保留 `lazy` 方案，避免首页再次被非首屏页签拖白。
- 根据用户补充的控制台警告，定位到 `frontend/src/views/BaseData.vue` 的股票池表格表头筛选：多个 `el-table-column` 在 `#header` 插槽中直接嵌复杂输入框，触发 Element Plus `TableTdWrapper` 的 “slot invoked outside render function” 警告，并可能连带影响页签切换时的表格 patch。已将列头筛选整体移到表格上方的独立筛选栏，表格列恢复为普通 label/prop，前端构建复查通过。
- 为继续抓首页白屏运行时首错，已在 `frontend/src/main.ts` 注入全局 `error` / `unhandledrejection` 控制台日志；同时启动本地前端开发服务 `http://127.0.0.1:4175` 和页面编辑器 `http://localhost:7788` 供浏览器侧排查。页面编辑器服务日志显示已启动，但端口文件未落到项目目录，当前转为让用户直接在浏览器打开编辑器并回传首条控制台报错，以便继续精确定位运行时异常。
- 根据用户最新反馈，首页本身不白，但切到“导入财报”会白。已继续收紧 `frontend/src/views/ImportWorkbench.vue` 的运行时保护：为 `openDocument` 增加浏览器环境判断、为 `loadDocumentPreview` 和 `documentRowClassName` 增加空行/异常保护并输出控制台错误，避免文档表格点击或高亮阶段在空节点状态下继续触发异常链；前端构建复查通过。
- 由于“导入财报”白屏时控制台没有任何报错，继续排除可能的挂载/Teleport 链路问题：移除了 `ImportWorkbench.vue` 中字段来源抽屉 `el-drawer` 的 `append-to-body`，避免切换页签时 Teleport 到 body 的抽屉节点残留；同时把 `watch(preview, syncEditablePreview)` 改成带 try/catch 的安全 watcher，异常时回退为空表单并打印控制台日志，避免预览同步阶段静默打断整页渲染；前端构建复查通过。
- 因前两轮运行时保护后“导入财报”仍白屏，继续用区块裁剪法定位：临时从 `ImportWorkbench.vue` 移除右侧复杂预览区、字段来源抽屉和导入批次表，仅保留文档资产库与隔离提示，用于判断问题是否来自右侧大表单/抽屉/批次区域；前端构建复查通过。
- 用户反馈裁掉右侧预览后“导入财报”仍白屏，进一步把 `ImportWorkbench.vue` 缩到更小：连文档资产库表格也临时移除，只保留左侧上传/手工补录和主舞台里的最小提示卡，用于继续判断问题是否来自左侧表单区或整体布局；前端构建复查通过。
- 当 `ImportWorkbench.vue` 已裁到只剩一张静态提示卡仍然白屏后，基本排除具体业务区块导致的可能。继续把 `frontend/src/App.vue` 中“导入财报”页签单独从 `lazy` 改回普通挂载，准备验证问题是否与该页签的 lazy 挂载机制本身有关；前端构建复查通过。
- 用户确认在将“导入财报”页签改成普通挂载后，不只是该页签白，而是整个首页都白；已立即将 `frontend/src/App.vue` 的“导入财报”页签恢复为 `lazy`，先恢复首页可用性。前端构建复查通过。
- 在保持首页和 `App.vue` 不再变动的前提下，继续用“极简版 → 逐块恢复”方法定位 `ImportWorkbench.vue`。本轮先恢复第一块纯静态说明文案，不包含任何上传、表单、表格、按钮或异步请求，只验证页面骨架和普通文本渲染链路；前端构建复查通过。
- 用户确认即使只恢复一段纯静态说明文案，“导入财报”页仍白屏。为进一步排除页面结构和样式层影响，已将 `ImportWorkbench.vue` 彻底缩成最小调试模板：空 `<script setup>` + 极简 `<template>` + 独立最小样式，不再引用任何 API、Element Plus 组件、响应式状态或旧布局类名；前端构建复查通过。
- 随后用户已能看到“导入财报页调试中”的最小静态模板文案，确认 `ImportWorkbench.vue` 当前模板本身可正常渲染，且 `App.vue` 中该页签继续保持 `lazy`；这说明此前白屏至少已不再由导入财报页的业务内容直接触发，下一步应从页签切换后的运行态、缓存/HMR 状态或逐块恢复内容回归验证继续定位。
- 继续按“最小模板 → 逐块恢复”推进导入财报页排查：本轮已在 `ImportWorkbench.vue` 最小静态模板上恢复纯静态头部区，包含标题、副文案和三步流程卡，但仍不引入任何表单、表格、按钮事件或异步请求；前端构建复查通过，可继续验证普通静态结构是否稳定。
- 在确认静态头部可显示后，继续恢复 `ImportWorkbench.vue` 左侧上传入口的静态展示外壳：包含上传区标题、PDF 拖拽区视觉、三个占位输入框和主按钮外观，但仍不接入上传组件、表单绑定、按钮事件或异步请求；前端构建复查通过。
- 继续按“静态外壳优先”恢复导入财报页：本轮在 `ImportWorkbench.vue` 左侧新增手工补录卡片，加入八个占位输入框和次级按钮外观，但仍不接入表单绑定、按钮事件或请求；上传区静态壳保持不变，前端构建复查通过。
- 继续恢复 `ImportWorkbench.vue` 右侧“文档资产库”的静态表格外壳：新增静态工具条、5列表头、3条模拟 PDF 文档记录和状态胶囊，但仍不接入真实数据、筛选、点击、预览联动或请求；前端构建复查通过，可继续验证该表格结构本身是否稳定。
- 继续恢复 `ImportWorkbench.vue` 的“解析预览区”静态外壳：新增静态指标概览卡、字段核对区、来源说明区和双按钮布局，但仍不接入 preview 数据、v-model、抽屉、确认动作或任何请求；前端构建复查通过，可继续验证预览区版式本身是否稳定。
- 根据用户取舍，已彻底取消“导入批次区”在导入财报页中的恢复计划；`ImportWorkbench.vue` 当前页面收口目标明确为“上传区 + 文档资产库 + 解析预览区”三块闭环，并同步更新调试文案，前端构建复查通过。
- 开始将导入财报页三块静态壳逐步恢复真实功能：第一阶段已为 `ImportWorkbench.vue` 上传区接回最小真实上传闭环，新增文件选择、代码/报告期/来源站点输入、调用 `api.uploadReportDocument` 的提交逻辑和上传结果摘要提示；仍未接回拖拽上传、资产库刷新联动和预览区同步，前端构建复查通过。
- 第二阶段已恢复 `ImportWorkbench.vue` 文档资产库的真实数据加载与基础筛选：组件挂载时调用 `api.getReportDocuments` 拉取文档列表，支持“全部/最近上传”切换和关键词过滤，列表按真实状态/更新时间渲染；上传成功后会自动刷新资产库。当前仍未接回点击预览、重解析和原文打开等高风险联动，前端构建复查通过。
- 第三阶段已恢复 `ImportWorkbench.vue` 解析预览区的只读真实功能：支持从文档资产库选择文档并调用 `api.getReportDocumentPreview` 加载真实预览，右侧展示真实指标摘要、只读财务字段和字段来源说明；暂不接回编辑态、字段来源抽屉和确认入库动作。期间修正了上传返回 `response.data` 可能为 null 的 TypeScript 空值错误，前端构建复查通过。
